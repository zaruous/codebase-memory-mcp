# Binaries
code-graph-mcp
/codebase-memory-mcp
bin/
*.exe
*.dll
*.so
*.dylib

# Test artifacts
*.test
*.out
coverage.txt

# Test fixture temp dirs (created by C test suite in CWD instead of /tmp/)
cbm_*/
cli-*/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Database files (local cache)
*.db
*.db-wal
*.db-shm

# MCP config (user-local, generated by install command)
.mcp.json

# MCP Registry auth tokens
.mcpregistry_*

# Local project memory (Claude Code auto-memory)
memory/
reference/

# Build artifacts
build/
node_modules/
graph-ui/dist/

# Generated reports
BENCHMARK_REPORT.md
TEST_PLAN.md
CHANGELOG.md

# Soak test output
soak-results/

# BGE-M3 extracted vectors (170MB binary — not tracked in git)
# Run scripts/setup-vectors.sh (or .ps1) to download or extract locally.
vendored/bge_m3_real/code_vectors.bin
vendored/bge_m3_real/*.npz
